schema_version: 1
# The hello-runtimes registry — the publish job fills this on each
# release (the registry lands by bot PR; main is protected). Until the
# first publish lands this document carries no payloads (a payload with
# an empty versions list is invalid — the validator fails closed).
payloads: []
