"""hello-python — the tebako sample app on the python runtime.

Stdlib only: the runtime edge is a version RANGE (pure-language law),
never an ABI line, so one payload rides every compatible python line.
"""

import platform
import sys

print(f"Hello from tebako (python {platform.python_version()}, {sys.platform})")
